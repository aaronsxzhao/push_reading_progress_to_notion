# ============================================
# Core Configuration (Required)

# 1. Notion token from: https://www.notion.so/my-integrations
NOTION_TOKEN=notion_token

# 2. Notion database ID (from the database URL)
NOTION_DATABASE_ID=notion_database_id

# ============================================
# Source Configuration (Required)
# ============================================
# Choose which source to sync: "weread" or "kindle" (for future use)
SYNC_SOURCE=weread

# Source label for WeRead books in Notion (default: "WeRead")
SOURCE_WEREAD=WeRead

# Source label for Kindle books in Notion (for future use)
# SOURCE_KINDLE=Kindle

# github gist token
GH_TOKEN="gist_token"

# ============================================
# WeRead Configuration (Required)

# 1. Get cookies
WEREAD_COOKIES="weread_cookies"

# Enable automatic cookie refresh when cookies expire (default: 1 = enabled)
# Set to 0 to disable automatic browser popup
WEREAD_AUTO_REFRESH_COOKIES=1

# ============================================
# Settings

# 1. Number of parallel workers for syncing (default: 5, max: 20)
WEREAD_MAX_WORKERS=5

# 2. Limit number of books to sync (for testing). Set to 1 to test with a single book, or remove/comment to sync all books
SYNC_LIMIT=1

# 3. Test with specific book title only (useful for debugging). Uncomment and set to test with one book:
WEREAD_TEST_BOOK_TITLE=test_book_title

# 4. Clear existing blocks when creating new pages (default: true). Set to false to append blocks instead of clearing
WEREAD_CLEAR_BLOCKS=true

# 5. Set to 1 to enable verbose debug output and warnings
WEREAD_DEBUG=1



# ============================================
# Web Server

# 1. Port for the local web server (default: 8765)
SYNC_SERVER_PORT=8765

# 2. Host for the local web server (default: 0.0.0.0)
# SYNC_SERVER_HOST=0.0.0.0

# 3. API key for securing the /sync endpoint (optional). If set, you must include X-API-Key header or api_key query param
# SYNC_API_KEY=your-secret-api-key-here




# ============================================
# Notion Property Names (Optional - Advanced)

# Only customize these if your Notion database uses different property names
# Default values are already set in the code, so you can remove these if using defaults

NOTION_TITLE_PROP=Title
# PROP_AUTHOR=Author
# PROP_STATUS=Status
# PROP_CURRENT_PAGE=Current Page
# PROP_TOTAL_PAGE=Total Page
# PROP_DATE_FINISHED=Date Finished
# PROP_SOURCE=Source
# PROP_STARTED_AT=Date Started
# PROP_LAST_READ_AT=Last Read At
# STATUS_TBR=To Be Read
# STATUS_READING=Currently Reading
# STATUS_READ=Read

# ============================================
# Choose which source to sync: "weread" or "kindle" (for future use)

# Source label for WeRead books in Notion (default: "WeRead")

# Source label for Kindle books in Notion (for future use)
# SOURCE_KINDLE=Kindle

# GitHub Gist for cookie sync
COOKIE_GIST_ID="cookie_gist_id"
